#include<raylib.h>
#define ScreenWidth 800
#define ScreenHight 600
int main(){
  InitWindow(ScreenWidth,ScreenHight,  "Brick Games v1.0");
  
  
  
  
  
  while(!WindowShouldClose()){
    BeginDrawing();
    EndDrawing();
  }
  CloseWindow();
  return 0;
}